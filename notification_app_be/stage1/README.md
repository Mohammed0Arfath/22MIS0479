# Stage 1 — Priority Inbox

Run the Stage 1 script which authenticates with the evaluation server, fetches notifications, and prints the top-10 priority notifications.

Requirements: Python 3.8+

Commands:

```bash
python stage1/priority_inbox.py
```
